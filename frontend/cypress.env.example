# Copy this file to cypress.env.json and fill in real credentials for environments
# that should run against live services. Values defined here are used by Cypress via
# process.env when running in CI.
CYPRESS_BASE_URL=http://localhost:3000
CYPRESS_API_URL=http://localhost:5000
CYPRESS_LOGIN_EMAIL=test.user@example.com
CYPRESS_LOGIN_PASSWORD=Password123!
